i feel like the agents.md is WAY TOO LONG, like supabase info should be in docs about the database, and then in 1 sentence or even just a bulltet points, this should be references in agents, like heres the info, so it doesnt have to read everything
so the agents.md contains the actual RULES, short and to the point, so the agents actually adhere to the rules, because now they just ignore any explicit instructions

/ponytail:ponytail-audit